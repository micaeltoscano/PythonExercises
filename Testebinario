num_bi= " " 

num= int(input("ditite o número a ser convertido para binario: "))

while num >0: 
 if num % 2 == 0: 
   num / 2
   num_bi += 0

 else: 
  (num - 1)/2
  num_bi += 1

print(f'{num_bi}')
    
  
